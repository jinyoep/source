CREATE TABLE YMS.U_PROD_OPER (
    FAB VARCHAR(10) NOT NULL,
    OPER VARCHAR(50) NOT NULL,    
    WF CHAR(2) NOT NULL,
    PROD VARCHAR(100) NOT NULL,
    ROW INTEGER DEFAULT 0,
    CNT SMAILINT,
    "TYPE" VARCHAR(20),    
    INNO DOUBLE,
    UPDATE_DT_TM TIMESTAMP,
    CONSTRAINT PK12334432423 PRIMARY KEY (FAB, OPER, PROD)
);

CREATE TABLE QDW.Q_INL_REP_DATA (
    FAB VARCHAR(10) NOT NULL,
    WF_YN CHAR(2) NOT NULL DEFAULT 'Y',
    ROW INTEGER,
    CNT SMAILINT,
    "TYPE" VARCHAR(20),    
    INNO DOUBLE,
    PRMT VARCHAR(100) NOT NULL,
    UPDATE_DT_TM TIMESTAMP,
    CONSTRAINT PK123581531186 PRIMARY KEY (FAB, PRMT, INNO)
);